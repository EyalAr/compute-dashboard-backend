describe('routes/login', () => {
  it('should return 401 if username is incorrect')
  it('should return 401 if password is incorrect')
  it('should return 204 and JWT header if credentials are correct')
});
